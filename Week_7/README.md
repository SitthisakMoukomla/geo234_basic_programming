# 🌍 การจัดการข้อมูล Raster ด้วย Python

## Rasterio, Xarray, Rioxarray และ XEE

**ผู้สอน:** ดร.สิทธิ์ เจตน์มงคล  
**สังกัด:** ภาควิชาภูมิศาสตร์ คณะศิลปศาสตร์ มหาวิทยาลัยธรรมศาสตร์

---

## 📖 ภาพรวม

Workshop นี้สอนการจัดการข้อมูล Raster ด้วย Python ตั้งแต่พื้นฐานจนถึงการประยุกต์ใช้งานจริง ครอบคลุม Library หลัก 4 ตัวใน Python Raster Ecosystem

```
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Rasterio │◄──►│  Xarray  │◄──►│   XEE    │
│ (I/O)    │    │ (N-dim)  │    │ (GEE)    │
└────┬─────┘    └────┬─────┘    └──────────┘
     └───────┬───────┘
      ┌──────┴──────┐
      │  Rioxarray  │  (สะพานเชื่อม)
      └──────┬──────┘
      ┌──────┴──────┐
      │    GDAL     │  (Engine หลัก)
      └─────────────┘
```

## 📂 โครงสร้างไฟล์

```
.
├── README.md                          ← ไฟล์นี้
├── raster_management_workshop.ipynb   ← Notebook หลัก (86 cells)
└── data/                              ← สร้างอัตโนมัติเมื่อรัน Notebook
    ├── dem_sample.tif                 ← DEM จำลองภาคเหนือ
    ├── multispectral_sample.tif       ← ภาพ 4 bands (B, G, R, NIR)
    ├── temperature_monthly.nc         ← อุณหภูมิรายเดือน (NetCDF)
    └── ... (ไฟล์ output ต่างๆ)
```

## 📋 เนื้อหา

| Part | หัวข้อ | เนื้อหาหลัก |
|------|--------|-------------|
| **1** | พื้นฐาน Raster | แนวคิด Raster, รูปแบบไฟล์, ติดตั้ง Library, สร้างข้อมูลตัวอย่าง |
| **2** | Rasterio | อ่าน/เขียน GeoTIFF, Window read, Reproject, Clip, Statistics, COG |
| **3** | Xarray | NetCDF, DataArray, Selection, GroupBy, Seasonal analysis, Dask |
| **4** | Rioxarray | เชื่อม Rasterio+Xarray, reproject_match, clip, export GeoTIFF |
| **5** | XEE | ดึง Sentinel-2, MODIS LST, SRTM DEM จาก Google Earth Engine |
| **6** | Workshop | NDVI Time-series, เปรียบเทียบ DEM, Raster Pipeline สมบูรณ์ |

## ⚙️ การติดตั้ง

### ขั้นต่ำ (Part 1–4)

```bash
pip install rasterio xarray rioxarray netcdf4 h5netcdf matplotlib numpy geopandas shapely dask[complete]
```

### เพิ่มเติมสำหรับ XEE (Part 5)

```bash
pip install xee earthengine-api
```

> **หมายเหตุ:** Part 5 (XEE) ต้องมี Google Earth Engine account และรัน `ee.Authenticate()` ก่อนใช้งาน สมัครได้ที่ https://earthengine.google.com

### เวอร์ชันที่แนะนำ

- Python ≥ 3.9
- Rasterio ≥ 1.3
- Xarray ≥ 2023.01
- Rioxarray ≥ 0.14

## 🚀 วิธีใช้งาน

1. เปิด `raster_management_workshop.ipynb` ใน Jupyter Notebook / JupyterLab / Google Colab
2. รัน Cell ติดตั้ง Library (Part 1.3)
3. รัน Cell สร้างข้อมูลตัวอย่าง (Part 1.4) — ข้อมูลจำลองจะถูกสร้างอัตโนมัติ ไม่ต้องดาวน์โหลด
4. เรียนรู้ตามลำดับ Part 1 → 6

> **สำหรับ Google Colab:** รันได้ทันที ไม่ต้องตั้งค่าเพิ่มเติม

## 🎯 ทักษะที่จะได้รับ

- อ่าน/เขียน GeoTIFF, NetCDF, COG ด้วย Python
- เข้าใจ CRS, Affine Transform, NoData
- Reproject และ Clip ข้อมูล Raster
- วิเคราะห์ Time-series ด้วย Xarray (GroupBy, Resample, Seasonal)
- จัดการข้อมูลขนาดใหญ่ด้วย Dask (Lazy Loading)
- ดึงข้อมูลจาก Google Earth Engine มาวิเคราะห์ใน Python
- คำนวณ Vegetation Indices (NDVI, SAVI, EVI)
- สร้าง Raster Processing Pipeline สมบูรณ์

## 📚 แหล่งเรียนรู้เพิ่มเติม

| แหล่ง | URL |
|-------|-----|
| Rasterio Documentation | https://rasterio.readthedocs.io |
| Xarray Documentation | https://docs.xarray.dev |
| Rioxarray Documentation | https://corteva.github.io/rioxarray |
| XEE GitHub | https://github.com/google/Xee |
| GEE Data Catalog | https://developers.google.com/earth-engine/datasets |
| Cloud-Native Geospatial | https://cloudnativegeo.org |
| Pangeo | https://pangeo.io |
